{
  "name": "abstract-lending",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat deploy-zksync --script deploy/deploy.ts --network abstractTestnet",
    "deploy:mainnet": "hardhat deploy-zksync --script deploy/deploy.ts --network abstractMainnet"
  },
  "devDependencies": {
    "@matterlabs/hardhat-zksync": "^1.0.0",
    "@types/node": "^20.0.0",
    "ethers": "^6.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0",
    "zksync-ethers": "^6.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
